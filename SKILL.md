---
name: free-llm-daily-check
description: '检索当日仍可免费使用的大语言模型，严格筛选永久免费或每日重置免费额度的模型，排除试用、限时、需付费、今日到期的资源。适用于用户询问免费AI模型推荐、免费大模型清单、不用付费的AI等场景。'
metadata:
  argument-hint: '[可选：具体需求描述]'
---

# 当日免费可用大模型检索 Skill

## 触发条件
当用户询问"今天有哪些免费大模型可以用"、"免费AI模型推荐"、"不用付费的AI"、"免费大模型清单"等类似需求时触发。

## 执行步骤

### Step 1: 多渠道检索
使用 `web_search` 进行多维度搜索，每次搜索设置 `mode: deep_research`：

**搜索维度：**
1. 英文永久免费API：`free LLM API permanent free tier 2026 no credit card`
2. 中文永久免费API：`免费大模型API 永久免费 2026 无需登录`
3. 在线免登录对话：`free AI chat no sign up no login 2026`
4. 国内可用免费模型：`国内免费大模型 永久免费 2026`
5. 聚合平台免费模型：`Poe.com free models OpenRouter free 2026`

**每次搜索结果后：**
- 对重要来源调用 `read_webpage` 验证实际内容和时效性
- 记录关键证据到证据库（如适用）

### Step 2: 硬性筛选（全部满足才收录）
每条模型必须同时满足以下4个条件：

1. **使用模式**：永久免费 / 每日重置免费额度。不接受一次性试用、新用户专享、限时活动。
2. **时效要求**：免费权益有效期 ≥ 今日。排除今日到期、试用截止的资源。
3. **可用性**：无需特殊资格认证、无地区锁（或中国大陆可直连）、能直接调用对话/推理。
4. **黑名单过滤**：
   - ❌ 仅新用户注册赠送额度（额度用完即止）
   - ❌ 需要信用卡验证的"免费"
   - ❌ 当日/当月有效限时活动
   - ❌ 额度耗尽后无法使用的模型
   - ❌ 需要付费会员才能使用的模型
   - ❌ 隐性扣费平台

### Step 3: 时效校验
对每个候选模型：
- 确认免费活动的持续状态（永久/每日重置/长期有效）
- 排除明确标注了截止日期的限时免费
- 注意区分"免费层"和"试用额度"

### Step 4: 可用性核验
- 优先收录官方API/控制台入口
- 标注是否需要注册账号
- 标注是否需要信用卡
- 标注中国大陆是否可直连
- 标注API兼容性（OpenAI兼容/原生SDK等）

### Step 5: 信息整理
- 去重合并相同模型的不同入口
- 按类型分类：API类 / 在线对话类 / 聚合平台类
- 标注每个模型的具体免费额度上限

## 输出格式

以Markdown表格输出，严格包含以下字段：

| 模型/平台名称 | 免费权益详情 | 使用入口 | 免费有效期 | 使用限制 |
|---|---|---|---|---|

**字段说明：**
- **模型/平台名称**：模型名或平台名
- **免费权益详情**：具体免费内容（如"DeepSeek R1永久免费"、"每日150条消息"）
- **使用入口**：官网URL或API接入方式
- **免费有效期**：永久免费 / 每日重置 / 具体截止日期
- **使用限制**：RPM、RPD、Token上限、是否需要注册等

**输出约束：**
- 只输出通过全部筛选条件的模型
- 禁止输出任何过期、付费、限时失效的模型
- 不编造不存在的免费资源
- 如果某个模型信息不确定，标注"需自行验证"
- 按类别分组输出（API类 / 在线对话类 / 聚合平台类）

## 注意事项
- 免费额度政策可能随时变动，提醒用户以官方最新说明为准
- 部分免费API可能使用用户数据进行模型训练，需注明
- 对于需要注册的免费服务，注明注册方式（邮箱/GitHub/手机号等）
